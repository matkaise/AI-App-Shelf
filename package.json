{
  "name": "ai-app-shelf",
  "version": "1.0.0",
  "description": "Self-hosted shelf for AI-generated single-file HTML apps.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/render.js && node --check scripts/smoke-test.js",
    "test": "node scripts/smoke-test.js",
    "test:online": "node scripts/smoke-test.js --online"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "esbuild": "^0.25.12",
    "express": "^4.22.1"
  }
}
